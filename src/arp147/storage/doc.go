/*
	Package storage contains a StorageCollection which links to different
	storage folders depending on the OS. This is used for storing both
	temporary and permanent files. Note that the user still CAN delete both
	the temporary and permanent files if they know where to look, but it's
	not likely they will.
*/
package storage
