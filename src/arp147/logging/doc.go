/*
	Package logging contains two loggers (printing to stdout and stderr) so
	that debug/Informative logging can be separate from error logging. In
	production mode, everything is printed to files instead of stdout/stderr.
*/
package logging
